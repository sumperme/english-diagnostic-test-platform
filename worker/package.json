{
  "name": "@edt/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev -c wrangler.toml --local --port 8787",
    "deploy": "wrangler deploy -c wrangler.toml",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260529.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.95.0"
  }
}
